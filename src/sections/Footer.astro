---
import { MapPin } from "@lucide/astro";
import { useTranslations } from "src/lib/i18n/utils";
import type { Language } from "src/types";

const currentLanguage: Language = (Astro.currentLocale || "en") as Language;
const t = useTranslations(currentLanguage);
---

<footer
  id="footer"
  class="text-slate-900 py-12 px-4 sm:px-6 lg:px-8 relative"
  role="contentinfo"
  aria-label={t("footer.ariaLabel")}
>
  <div class="max-w-6xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
      <div class="flex flex-col gap-4">
        <h2 class="text-2xl font-bold font-mono">Marcelo Oroquieta</h2>
        <p class="text-gray-700 font-mono text-sm">
          {t("footer.description")}
        </p>
        <div class="flex items-center gap-2 text-gray-400">
          <MapPin size={16} aria-hidden="true" />
          <span class="font-mono text-sm">{t("footer.location")}</span>
        </div>
        <div
          class="flex items-center gap-2 text-emerald-500 font-mono text-sm pl-[3px]"
        >
          <div class="relative w-2.5 h-2.5">
            <span
              class="absolute w-2.5 h-2.5 rounded-full bg-emerald-400 animate-[ping_2s_cubic-bezier(0,0,0.2,1)_infinite]"
              shadow-[0_0_10px_#5EEA6F]></span>
            <span class="relative flex h-2.5 w-2.5 rounded-full bg-emerald-500"
            ></span>
          </div>
          {t("footer.available")}
        </div>
      </div>

      <!-- Quick links -->
      <div class="flex flex-col md:items-end gap-4">
        <h3 class="text-lg font-semibold font-mono">{t("footer.linksTitle")}</h3>
        <nav aria-label={t("footer.nav.ariaLabel")}>
          <ul class="flex flex-col md:items-end gap-2">
            <li>
              <a
                href="#hero"
                class="text-gray-700 hover:text-blue-500 hover:font-medium transition-colors font-mono text-sm block"
                aria-label={t("footer.nav.hero.ariaLabel")}
              >
                {t("footer.nav.hero")}
              </a>
            </li>
            <li>
              <a
                href="#experience"
                class="text-gray-700 hover:text-blue-500 hover:font-medium transition-colors font-mono text-sm block"
                aria-label={t("footer.nav.experience.ariaLabel")}
              >
                {t("footer.nav.experience")}
              </a>
            </li>
            <li>
              <a
                href="#projects"
                class="text-gray-700 hover:text-blue-500 hover:font-medium transition-colors font-mono text-sm block"
                aria-label={t("footer.nav.projects.ariaLabel")}
              >
                {t("footer.nav.projects")}
              </a>
            </li>
            <li>
              <a
                href="#contact"
                class="text-gray-700 hover:text-blue-500 hover:font-medium transition-colors font-mono text-sm block"
                aria-label={t("footer.nav.contact.ariaLabel")}
              >
                {t("footer.nav.contact")}
              </a>
            </li>
          </ul>
        </nav>
      </div>
    </div>

    <!-- Divider -->
    <div class="border-t border-gray-200 pt-8">
      <div
        class="flex w-full justify-center gap-2 text-gray-400 font-mono text-sm"
      >
        {t("footer.credit").replace("{name}", "Marcelo Oroquieta")}
      </div>
    </div>
  </div>
</footer>

<style>
  /* Focus styles for better accessibility */
  footer a:focus {
    outline: 2px solid #60a5fa;
    outline-offset: 2px;
    border-radius: 2px;
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    footer {
      border-top: 2px solid white;
    }
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    * {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>
